INSERT INTO `system_email` (`template_id`, `template_path`, `desc`, `subject`, `sender`, `recipient`, `copied`, `copied_blind`) VALUES
('lead.fail.re', 'lead/failed-report', 'sending a link to download report which contains lead list were not sent to the CRM from JAWS due to some issue', ' Failed Leads Report', 'info@jigsawacademy.com,Jigsaw Academy', 'programoffice@jigsawacademy.com', '', 'medini.m@digital.datamatics.com,k.aishwarya@digital.datamatics.com');
-- change the comment for status column in user_leads_basc table
ALTER TABLE `user_leads_basic` CHANGE `status` `status` SMALLINT(6) NOT NULL DEFAULT '0' COMMENT '0 = new data, 1 - processed , 9 = old data,6 -others';ALTER TABLE `user_leads_basic` CHANGE `status` `status` SMALLINT(6) NOT NULL DEFAULT '0' COMMENT '0 = new data, 1 - processed , 9 = old data,6 -others';ALTER TABLE `user_leads_basic` CHANGE `status` `status` SMALLINT(6) NOT NULL DEFAULT '0' COMMENT '0 = new data, 1 - processed , 9 = old data,6 -others';ALTER TABLE `user_leads_basic` CHANGE `status` `status` SMALLINT(6) NOT NULL DEFAULT '0' COMMENT '0 = new data, 1 - processed , 9 = old data,6 -others';ALTER TABLE `user_leads_basic` CHANGE `status` `status` SMALLINT(6) NOT NULL DEFAULT '0' COMMENT '0 = new data, 1 - processed , 9 = old data,6 -others';ALTER TABLE `user_leads_basic` CHANGE `status` `status` SMALLINT(6) NOT NULL DEFAULT '0' COMMENT '0 = new data, 1 - processed , 9 = old data,6 = others';